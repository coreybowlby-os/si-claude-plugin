interface:
  display_name: "SI Claude Plugin"
  short_description: "Repo-specific patterns and workflows for SI-Claude-Plugin"
  default_prompt: "Use $SI-Claude-Plugin to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true